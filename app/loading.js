const Loading = () => {
    return (
        <div className="min-h-screen z-50 w-full bg-gradient-to-b from-[#071417] via-[#1A3E40] to-[#133E3F] fixed flex justify-center items-center">
            <div className="loader"></div>
        </div>
    );
}

export default Loading;